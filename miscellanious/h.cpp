#include<stdio.h>
#include<conio.h>
main()
{
	int a,b,sum;
	printf("the first number is ");
	scanf("%d",a);
	printf("the second number is ");
	scanf("%d",b);
	sum=a+b;
	printf("sum is=%d\n",sum);
	getch();
}
