#include<stdio.h>
struct tag 
{
	int a[100];
	int size;
};
int main()
{
	struct tag node[100];
	scanf("%ld",&c);
	for(t=1;t<=c;t++)
	{
		scanf("%ld %ld",&n,&e);
	    for(i=0;i<e;i++)
		{
			scanf("%ld %ld",&p,&q);
		    node[p].a[p.size]=q;
		    p.size++;
		}
	    scanf("%ld %ld",&s,&qry);
		for(q=0;q<qry;q++)
		{
			scanf("%ld",&v);
			if(v==1)
			{
			}
			else if(v==2)
			{
			}
			else if(v==3)
			{
			}
		}
	}
	return 0;
}

	}
